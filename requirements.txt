# Analysis and figure pipeline (stages 1-7; stage 8 needs the optional block below).
#
# The committed results in results/ were produced with the versions marked "used:". The same
# numbers also came out under pandas 3.0.1 / scipy 1.17.1 / scikit-learn 1.8.0, agreeing to
# 8.9e-16 and rendering an identical manuscript, so nearby versions are fine - and
# checks/check_repo.py will tell you if yours are not.
pandas>=1.3           # used: 2.3.3
numpy>=1.20           # used: 2.0.2
scipy>=1.7            # used: 1.13.1   (Spearman correlations, stages 5 and 7)
scikit-learn>=1.0     # used: 1.6.1    (standardised partial AUC, stage 5)
openpyxl>=3.0         # used: 3.1.5    (reads the raw .xlsx workbooks in stage 1)
PyYAML>=5.4           # used: 6.0.3    (config/benchmark_config.yaml)

# Optional - stage 8 supplementary figures only
matplotlib>=3.4       # used: 3.9.4
Pillow>=8.0           # used: 11.3.0
cairosvg>=2.5         # used: 2.8.2

# Python 3.9 or newer (used: 3.9.24).
#
# LaTeX, for the figure images and the manuscript PDF: TeX Live with pgfplots >= 1.18 (pdflatex,
# bibtex), or Docker - build_manuscript.sh falls back to the texlive/texlive image (TeX Live 2026,
# pgfplots 1.18.2 used).
